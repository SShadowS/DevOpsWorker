# CRITICAL: LSP Tool Usage Required

You MUST use the LSP tool for ALL AL code navigation. Do NOT use Grep or Glob to find procedures, definitions, references, or type information in .al files. The LSP tool provides compiler-accurate results in milliseconds. Grep is only for non-code text (comments, strings, TODOs).

Failure to use LSP for code navigation will result in rejection of your output.